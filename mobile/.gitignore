node_modules/
# Static export copied in from ../frontend by `npm run copy:web`. Build
# output, never edited here and never committed.
www/
# Generated by `npx cap add android`. Committed deliberately (see README) --
# these are the local build artefacts inside it that are not.
android/build/
android/app/build/
android/.gradle/
android/local.properties
# Signing material. Never commit a keystore.
*.keystore
*.jks
keystore.properties
